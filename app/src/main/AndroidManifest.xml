<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="monster.myapp.moviecatalogue">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieCatalogue">
        <activity
            android:name=".setting.SettingsActivity"
            android:label="@string/change_settings"/>
        <activity android:name=".main.MainActivity" />
        <activity
            android:name=".detail.DetailCatalogueActivity"
            android:theme="@style/Theme.MovieCatalogue.NoActionBar" />
        <activity
            android:name=".main.SplashActivity"
            android:noHistory="true"
            android:theme="@style/Theme.MovieCatalogue.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>